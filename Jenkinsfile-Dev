pipeline{
    stages{
        stage("Message"){
            echo "Starting...."
        }
        stage("createFolder"){
            mkdir tetsPipeline
        }
        stage("Clone"){
            sh 'python --version'
        }
    }
}
