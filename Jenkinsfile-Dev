pipeline {
    agent { docker { image 'python:3.5.1' } }
    stages {
        stage('createFolder') {
            steps {
            cd \
                mkdir pythonBackup
            }
        }
    }
}
