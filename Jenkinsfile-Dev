pipeline {
    agent any
    stages {
        stage('install') {
            steps {
                sh 'git clone https://github.com/anubandara/python-helloworld.git'
            }
        }
    }
}
